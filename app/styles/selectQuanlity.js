import {StyleSheet} from 'react-native';

const selectQuanlity = StyleSheet.create({
  selectContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    width: 80,
  },
  quanlityDish: {
    color: 'white',
    fontSize: 20,
    textAlign: 'center',
    fontFamily: 'notoserif',
  }
});

export default selectQuanlity;
